apiVersion: v1
kind: ServiceAccount
metadata:
  name: tenant-{{ .Values.tenant.slug }}-sa
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "soctalk-tenant.labels" . | nindent 4 }}
automountServiceAccountToken: false
